# Milestone 1 Architecture

Flow: run.bat -\> launcher.py -\> startup checks -\> hardware detection
-\> dependency verification -\> start local server -\> open browser

No AI engines are implemented in this milestone.
